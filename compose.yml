services:
  bird-app:
    build: .
    ports:
      - "0:8501"
    volumes:
      - ./data:/app/data
      - ./audio_files:/app/audio_files
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - PYTHONUNBUFFERED=1