services:
  bird-app:
    build: .
    container_name: bird-audio-app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./audio_files:/app/audio_files
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - PYTHONUNBUFFERED=1